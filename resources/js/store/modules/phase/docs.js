const freshState = () => ({
    active: null,
  sections: {},
    versions: []
});

const getters = {
  //
};

const mutations = {
  //
};

const actions = {
  //
};

export default {
  namespaced: true,
  state: freshState(),
  getters,
  mutations,
  actions
};
